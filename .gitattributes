# Normalize text files to LF in the repository (Windows checkouts still work).
* text=auto eol=lf

# Recorded NCBI responses must stay byte-for-byte identical to what was downloaded.
snapshots/** -text
*.xlsx binary
*.png binary
